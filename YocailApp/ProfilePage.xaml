<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:YocailApp.ViewModel"
             x:Class="YocailApp.ProfilePage" Title="Profile">
    <ContentPage.BindingContext>
        <local:ProfileVM />
    </ContentPage.BindingContext>
    <ScrollView>
        <VerticalStackLayout>
            <Frame IsVisible="{Binding ProfilePicVisible}" CornerRadius="10" BorderColor="Transparent" Padding="0" WidthRequest="100" HeightRequest="100" 
                       VerticalOptions="Center" HorizontalOptions="Center">
                <Image Source="{Binding ProfilePic}" Aspect="AspectFit" WidthRequest="100" HeightRequest="100" VerticalOptions="Center" HorizontalOptions="Center" />
            </Frame>
            <StackLayout HeightRequest="5"></StackLayout>
            <Label Text="{Binding UserName}" HorizontalOptions="Center" FontFamily="InterRegular" FontSize="19" ></Label>
            <StackLayout HeightRequest="5"></StackLayout>
            <Label Text="{Binding Name}" IsVisible="{Binding NameVisible}" FontFamily="InterRegular" FontSize="19" FontAttributes="Bold" 
                   HorizontalOptions="Center"></Label>
            <StackLayout HeightRequest="8"></StackLayout>
            <Label Text="{Binding ThoughtStatus}" IsVisible="{Binding ThoughtStatusVisible}" FontFamily="InterRegular" FontSize="17" HorizontalOptions="Center" ></Label>
            <StackLayout HeightRequest="5"></StackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>